## Skills
Load `frontend-style`, `bun`, `tdd` before any work.

## Gates
All pass before commit:
- `bun run lint` (Biome: `bunx --bun @biomejs/biome check .`)
- `bun run build` (`next build --webpack`)
- `bun test`
